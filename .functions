# Sets system volume to $number on a scale of 0 to 10.
function vol() {
  osascript -e "set volume ${1}"
}

# Search git commits that match `$pattern` while ignoring case.
function glgrep() {
  git log --grep="${1}" --regexp-ignore-case
}