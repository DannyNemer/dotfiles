Install packages:
	sh install.sh

Set up Dropbox (after installing via `brew`).

Create symbolic links for dotfiles:
	sh link.sh

Set up Alfred sync.

Sync Desktop with Dropbox.

Configure OS settings (after setting up Dropbox with Finder wallpaper):
	sh macos.sh

Install Mac App Store applications:
	Pixelmator.app

Sign in: Google Chrome, Spotify, RescueTime, VS Code.
